---
/**
 * index.astro · 主页（v0.3 · Phase 2 §0 第一刀）
 *
 * 职责：从 Base 拉全站布局，渲染 Cover.astro。
 *
 * v0.3 变化：
 *   - v0.2 内联的 welcome / Forever Begins / 新人 / 时间地点 / 诗句 整体迁入 src/components/Cover.astro
 *   - 邀请函 part_1 通过 <CdnImage cdnTarget="misc" stem="invitation/part_1"> 首次接入主仓
 *   - 满足 §5.2 #4b CdnImage runtime gate（v1.39 审计 P2 修订）
 *
 * 后续在 Phase 2 §0 内逐步叠：
 *   - §0 第二刀：含羞草花瓣 Canvas 粒子（轻量 / 可关）
 *   - §0 第三刀：mouse parallax / device-orientation
 *   - §0 第四刀：水彩纸纹背景 + Mimosa / Olive Branch SVG motif
 *
 * 当前页面没有任何 inline `font-family` / `vw` / 负 letter-spacing；排版常量
 * 全走 §1.1.10–§1.1.12 的 token + helper class。
 */
import Base from "@/layouts/Base.astro";
import Cover from "@/components/Cover.astro";
---

<Base>
  <Cover />
</Base>
