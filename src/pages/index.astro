---
/**
 * index.astro · 主页（v0.5 · Phase 2 §0 + §1 + §2 batch 1 接入）
 *
 * 职责：从 Base 拉全站布局，按 DESIGN §3.1 站点地图顺序渲染章节。
 *
 * 当前章节顺序（自上而下，单页长卷）：
 *   §0  Cover         邀请函 part_1 + Forever Begins hero + 新人 / 时间 / 地点 / 诗句
 *   §1  Invitation    邀请函 part_2 + "我们无法判断..." 诗句 + 倒计时
 *   §2  Story         爱情独白长卷 batch 1（前 4 个 photo-poem beat + globe/finale 占位）
 *                     · DESIGN §4 §2.A StoryPoemScroller
 *                     · ⏳ batch 2: 后 6 个 photo-poem · ⏳ batch 3: §2.B Globe · ⏳ batch 4: §2.C Finale
 *   §3  Galleries     （未实施 · 已并入 §2）
 *   §4  Cats          （未实施 · DESIGN §4 §4）
 *   §5  Day           （未实施 · 婚礼详情 + 导航）
 *   §6  Closing       （未实施 · 落款）
 *
 * v0.5 变化：
 *   - v0.4 渲染 Cover + Invitation；v0.5 在 Invitation 之后追加 <StoryPoemScroller />
 *   - 三个章节都是独立 stacking context（each section: position: relative; isolation: isolate）
 *   - 不在此层注入装饰；装饰由各章节自己组织（§0 WatercolorPaper 等）
 *
 * v0.4 历史：
 *   - 在 Cover 之后追加 <Invitation />（§1 第一刀：part_2 + 诗句 + 倒计时）
 * v0.3 历史：
 *   - v0.2 内联的 welcome / Forever Begins / 新人 / 时间地点 / 诗句 整体迁入 src/components/Cover.astro
 *   - 邀请函 part_1 通过 <CdnImage cdnTarget="misc" stem="invitation/part_1"> 首次接入主仓
 *
 * 当前页面没有任何 inline `font-family` / `vw` / 负 letter-spacing；
 * 排版常量全走 §1.1.10–§1.1.12 的 token + helper class。
 */
import Base from "@/layouts/Base.astro";
import Cover from "@/components/Cover.astro";
import Invitation from "@/components/Invitation.astro";
import StoryPoemScroller from "@/components/story/StoryPoemScroller.astro";
---

<Base>
  <Cover />
  <Invitation />
  <StoryPoemScroller />
</Base>
